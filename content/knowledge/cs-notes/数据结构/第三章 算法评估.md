![[resource/Pasted image 20240326213616.png]]

一、算法时间复杂度
定义：事前预估算法时间开销T(n)与问题规模难度关系（T：time）
  T(n)=O(n)：算法的时间开销数量级。
  加法规则：
  T(n)=T<sub>1</sub>(n)+T<sub>2</sub>(n)=O(f(n))+O(g(n))=O(max(f(n),g(n)))
  乘法规则：
  T(n)=T<sub>1</sub>(n) X T<sub>2</sub>(n)=O(f(n)) X O(g(n))=O(f(n)) X g(n))
各数量级从小到大排序：
O(1) < O(log<sub>2</sub>n) < O(n) < O(nlog<sub>2</sub>n) < O(n<sup>2</sup>) < O(n<sup>3</sup>) < O(2<sup>n</sup>) < O(n!) < O(n<sup>n</sup>)

实际分析：最好时间复杂度，最坏时间复杂度，平均时间复杂度

总结：
![[resource/Pasted image 20240326220522.png]]

二、空间复杂度
1、普通程序
![[resource/Pasted image 20240329164158.png]]
占用空间大小同时间复杂度一样，同问题规模大小有关

2、递归程序
![[resource/Pasted image 20240329164606.png]]
函数调用也会占用内存，递归调用应为O(n)的复杂度，每层调用也可能会声明不同内存的空间调用

总结：
![[resource/Pasted image 20240329164922.png]]

