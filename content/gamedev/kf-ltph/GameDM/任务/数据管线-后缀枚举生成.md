---
type: task
status: done
requester: lead
start: 2026-09-04
end: 2026-09-04
depends: [数据管线-规则二批与导出原子性]
milestone: false
---

# 数据管线：`>类型` 后缀在单选列上生成枚举（而非仅引用）

## 背景与拍板

规则二批把单选列 `op>CompareOp` 实现为"引用既有枚举、不生成"（CompareOp 实际存在于 Packages/com.kframework）。负责人裁定改为**生成**：后缀=枚举类名，选项=枚举成员——与"零项目知识"原则一致（引用语义隐含项目已有该类型，属项目知识）。表选项已核实：两张表各 6 个选项 GreaterEqual/Greater/Great/Equal/Less/LessEqual/NotEqual，合法标识符且跨表一致。

## 交付物

1. `duowei_export.py` `_make_member` 单选分支：`ename = spec.type_hint or pascal_cs(out)`——带后缀时枚举以**后缀名**注册进 enum_registry（跨表选项不一致沿用既有 FAIL）并作为成员类型；无后缀维持按字段名生成。json 侧不变（仍输出选项名）。
2. 重导出：`Generated/Enum/` 出现 `CompareOp.cs`（6 成员）；PropertyCompare.op/EventListener 相关不变；FAIL=0 WARN=1（"类"回退 string 预期）。
3. 冒烟门禁：`ExportCheck/Shims.cs` **删除 CompareOp 垫片**（生成物自带，留着会 CS0101）；重跑门禁 23 项全 PASS。
4. 注释与 README 同步：`>类型` 枚举语义改为"以该名生成枚举"。

## 验收标准

- Enum 目录 = KillMode.cs + CompareOp.cs，CompareOp 成员与表选项一致
- 门禁全 PASS；导出可重复
- 红线：不 git commit；只改 LTPHDoc/GameData/；不动 .duowei

## 验收记录

- 2026-09-04 队长验收：CompareOp.cs 生成（6 成员与表选项一致，`public CompareOp op` 与项目 EventFieldCompare 对齐）；导出 FAIL=0 WARN=1；门禁 23/23；两轮导出 37 产物 md5 稳定；追加轮修复枚举成员缩进统一（仅 2 枚举文件变化，其余 35 文件字节级一致）

## 备忘（P2 替换阶段）

生成 CompareOp 入 Assets 时与 Packages/com.kframework 既有 CompareOp 撞名（同全局命名空间、Assembly-CSharp 引用 KFramework.Runtime 时用点会 CS0104）——届时二选一：删框架侧该枚举（迁移 FlowTransition 引用）或数据表换后缀引用。

## 关联

- 前置：`任务/数据管线-规则二批与导出原子性`（已完成）
- 记忆：`duowei-export-rules-v2-2026-09-04`
