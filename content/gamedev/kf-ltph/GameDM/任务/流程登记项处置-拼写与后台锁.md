---
type: task
status: done
requester: task
start: 2026-09-05
end: 2026-09-05
depends: []
milestone: false
---

# 流程登记项处置：Eneter 拼写改名 + 后台态锁玩家输入

## 拍板（负责人 2026-09-05）

- 登记 #1：`Game_Eneter` 改名 `Game_Enter`；`Flow_Game.prefab` 子物体 `Disiable` 改名 `Disable`
- 登记 #3：后台界面期间**需要锁玩家操作**——Scene_Backend.OnEnter 改 `SetPlayerInputStatus(true)`（禁用）、OnExit 改 `SetPlayerInputStatus(false)`（恢复）

## 交付物

1. `Assets/Scripts/Flow/GameFlow/Game_Eneter.cs` → 文件与类名改 `Game_Enter`（保 GUID：改文件名+类名，meta 不动；grep 全部引用同步，含 prefab 内脚本引用按 GUID 应不断，验证编译）。同 prefab 子物体 `Disiable` 文本改名 `Disable`（prefab YAML 内 m_Name，先 grep 确认无按名引用）。
2. `Scene_Backend.cs`：OnEnter `SetPlayerInputStatus(false)`→`true`、注释同步（"后台界面期间锁定玩家输入"）；OnExit 增加 `SetPlayerInputStatus(false)` + 注释（"后台关闭恢复玩家输入"；置于 ForceDepthOneNext 行旁）。迁移时序核查：Backend→Loading 迁移时 OnExit 解锁与 Loading.OnEnter 加锁同帧先后，确认无输入泄漏窗口（如有则说明处理）。
3. 双 csproj 0 error；grep `Game_Eneter|Disiable` 清零。
4. 文档同步（另派 plan）：场景流程.md 后台行、玩家流程总览 输入锁联动段补"后台态亦锁"。

## 验收记录

- 2026-09-05 队长验收：Game_Enter 改名 GUID 保留（prefab 引用不断）、prefab 子物体 Disable、Scene_Backend 进后台锁输入/退出恢复；同栈迁移证实无输入泄漏窗口（Exit→Enter 同调用栈无帧边界）；双 csproj 0 error、grep 零残留；文档同步已另派 plan

## 验收

- 改名零残留、构建过、后台锁行为生效；Unity 侧 prefab 引用不断（负责人复验）

## 关联

- 前置：流程文档对齐代码（登记清单）
