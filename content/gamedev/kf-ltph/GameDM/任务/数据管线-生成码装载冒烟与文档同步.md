---
type: task
status: done
requester: lead
start: 2026-09-04
end: 2026-09-04
depends: []
milestone: false
---

# 数据管线：生成码装载冒烟门禁重建 + 规则文档同步

## 目标

直出管线 v2 规则落地后（见记忆 `duowei-export-rules-v2-2026-09-04`），重建生成产物的**编译 + json 装载冒烟门禁**（原 ExportCheck 随目录整理移除），并把新规则同步进工具与使用文档。

## 交付物

### A. 冒烟门禁（主交付）

位置 `GameData/tools/Program/ExportCheck/`（dotnet 控制台），功能：

1. **编译面**：把 `GameData/Export/Generated/` 五目录全部 .cs 编入门禁工程；生成类现为全局命名空间 + `using KFramwork;`，依赖的 ActorData/AbstractData 用最小 shim（或引用 `Packages/com.kframework` 对应源文件，二选一取更稳）；`Vector2` 用同构 shim（x/y public field，供 Newtonsoft `{"x","y"}` 直读）
2. **装载面**：Newtonsoft 反序列化 `GameData/Export/` 全部 5 个 json，断言：
   - 条数：GroupData=1220 / TimelineEntry=864 / ConditionEvent=556 / PathConfig=81 / FishWaveData=40
   - GroupData 子类 `__type` 分布：SchoolGroupData=168 / FormationGroupData=955 / QueueGroupData=80
   - ConditionEvent：全部记录含平铺 `Count` 字段
   - PathConfig：Custom 记录 `Points` 装载为 Vector2 列表、Random 记录 `BoundInner/BoundOuter` 装载为 Vector2
   - FishWaveData：`TimelineEntry` 嵌套列表装载成功
3. **输出**：逐项 PASS/FAIL 汇总 + 退出码（0=全过 / 1=有 FAIL）
4. **运行方式**：文档库根 `dotnet run --project GameData/tools/Program/ExportCheck`
5. 历史参考：`git log --all --oneline -- '*ExportCheck*'` 可恢复原工程作参考（原位于 DragonBreakSeaHPGamepalyDoc/GameData/tools/ExportCheck）

### B. 文档同步（次交付）

1. `GameData/tools/Program/duowei_export.py` 头部规则注释补齐现状：`>类型` 后缀、记录类型表类型源列、`using` 替代 namespace（全局命名空间）、`:struct` 组 json 平铺
2. `GameData/README.md`：速查段补一句新规则说明（>类型 后缀与类型源列的用法）

## 验收标准

- `dotnet run` 全断言 PASS（FAIL=0），可重复执行
- duowei_export.py 与 README 描述与代码现状一致（无过时表述）
- 不碰 `Assets/`、不改 `.duowei` 表数据、不 git commit

## 验收记录

- 2026-09-04 队长验收：22 项断言 PASS（交付时产物快照：31 cs，事件监听中间态）；文档同步完成
- 已知衔接项：任务期间产物被表格改名中间态再次劣化（两张明细表断链），门禁编译暂红——由后续任务 `数据管线-规则二批与导出原子性` 修复产物后恢复绿色（验证含门禁复跑）

## 关联

- 产物与规则背景：记忆 `duowei-export-rules-v2-2026-09-04`、`bridge-merged-export-backend-8712-2026-09-04`
- 上游已完成：导出器 v2 升级 + 复比对（MATCH 15/32，剩余差异已归类设计使然）
